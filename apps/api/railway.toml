[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile.api"

[deploy]
startCommand = "npx prisma db push --schema=src/prisma/schema.prisma --accept-data-loss && node dist/src/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

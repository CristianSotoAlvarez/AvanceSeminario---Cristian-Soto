[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile.api"

[deploy]
startCommand = "npx prisma migrate deploy --schema=src/prisma/schema.prisma && node dist/src/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
